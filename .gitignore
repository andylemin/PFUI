# Build and interpreter artefacts
__pycache__/
*.py[cod]
.pytest_cache/
build/
dist/
*.egg-info/

# Cargo build output. Cargo.lock IS committed: the daemon is a binary, and the
# installer builds it with --locked
target/

# The installer creates a virtualenv here if run from the repo root
pfui_firewall/

# Local agent/editor state, never committed
.roo/
.DS_Store
